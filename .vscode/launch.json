{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "chrome",
      "request": "launch",
      "name": "Client",
      "url": "https://localhost:3000/",
      "webRoot": "${workspaceFolder}",
      "preLaunchTask": "npm: start"
    }
  ]
}
